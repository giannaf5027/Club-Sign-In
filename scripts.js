// poker logic


//blackjack logic


//codes

function codes(){
    getelementid()
    if (input == "StopTheShow!"){
        coin += 200;
        code == null;
    }
}

addEventListener();{
    if(null){
        print();
    }
}

function welcome() {
    setScreen("home");
    console.log (("Hello " + nombre) + "! Pick lost or added to see the data for each.");
  }
  
  function logout() {
    setScreen("home");
    hideElement("addb");
    hideElement("lostb");
    hideElement("datalbl");
    showElement("insertname");
    showElement("login");
  }

